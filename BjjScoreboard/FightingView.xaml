<UserControl x:Class="BjjScoreboard.FightingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1200">

    <UserControl.Resources>
        <!-- Обновленный стиль кнопок-переключателей для Иппонов -->
        <Style x:Key="IpponToggle" TargetType="ToggleButton">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <!-- Оптимальный размер шрифта -->
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border" Background="#EEEEEE" BorderThickness="0" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFCC00"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- ================= КРАСНЫЙ БОЕЦ ================= -->
        <Border Grid.Row="0" Background="{Binding RedFighter.RowBackground}" CornerRadius="4" Margin="0,0,0,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="160" />
                </Grid.ColumnDefinitions>

                <!-- Баллы -->
                <Grid Background="#DC143C">
                    <TextBlock Text="{Binding RedFighter.Points}" Foreground="White" FontSize="110" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,35"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,5">
                        <Button Content="+1" Focusable="False" Command="{Binding RedFighter.ModifyPointsCmd}" CommandParameter="1" Width="45" Height="30" Margin="2" FontWeight="Bold"/>
                        <Button Content="+2" Focusable="False" Command="{Binding RedFighter.ModifyPointsCmd}" CommandParameter="2" Width="45" Height="30" Margin="2" FontWeight="Bold"/>
                        <Button Content="-1" Focusable="False" Command="{Binding RedFighter.ModifyPointsCmd}" CommandParameter="-1" Width="45" Height="30" Margin="2" FontWeight="Bold" Background="#7F0000" Foreground="White"/>
                    </StackPanel>
                </Grid>

                <!-- Автоматический блок Full Ippon -->
                <Grid Grid.Column="1" Background="#2A3B50" Margin="5,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="FULL IPPON (ИНДИКАТОРЫ)" Foreground="#8A99AD" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,4,0,0"/>

                    <!-- Сетка с одинаковыми строками и колонками для идеального выравнивания -->
                    <Grid Grid.Row="1" Margin="5,3,5,5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- P1 и P2 теперь объединяют 2 строки (RowSpan="2"), чтобы быть высокими и красивыми -->
                        <ToggleButton Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Content="P1 (+2)" IsChecked="{Binding RedFighter.Part1Ippon}" Style="{StaticResource IpponToggle}"/>
                        <ToggleButton Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Content="P2 (+2)" IsChecked="{Binding RedFighter.Part2Ippon}" Style="{StaticResource IpponToggle}"/>

                        <!-- P3 разделены на две аккуратные строчки справа -->
                        <ToggleButton Grid.Row="0" Grid.Column="2" Content="P3 (+2)" IsChecked="{Binding RedFighter.Part3Ippon2Points}" Style="{StaticResource IpponToggle}"/>
                        <ToggleButton Grid.Row="1" Grid.Column="2" Content="P3 (+3)" IsChecked="{Binding RedFighter.Part3Ippon3Points}" Style="{StaticResource IpponToggle}"/>
                    </Grid>
                </Grid>

                <!-- Наказания -->
                <Grid Grid.Column="2" Background="#FFCC00" Margin="0,0,5,0">
                    <TextBlock Text="{Binding RedFighter.Penalties}" Foreground="Black" FontSize="65" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,30"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,5">
                        <Button Content="+" Focusable="False" Command="{Binding RedFighter.ModifyPenaltiesCmd}" CommandParameter="1" Width="40" Height="30" Margin="2" FontWeight="Bold"/>
                        <Button Content="-" Focusable="False" Command="{Binding RedFighter.ModifyPenaltiesCmd}" CommandParameter="-1" Width="40" Height="30" Margin="2" FontWeight="Bold"/>
                    </StackPanel>
                    <TextBlock Text="Наказания" Foreground="#222" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,38"/>
                </Grid>

                <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBox Text="{Binding RedFighter.Name, UpdateSourceTrigger=PropertyChanged}" Tag="MainWin"
                             Foreground="White" FontSize="48" FontWeight="Bold" Background="Transparent" BorderThickness="0" CaretBrush="White"/>
                    <ComboBox ItemsSource="{Binding Teams}" SelectedItem="{Binding RedFighter.Team}" Focusable="False"
                              Width="300" FontSize="24" HorizontalAlignment="Left" Margin="0,5,0,0" Background="#172637" Foreground="Black"/>
                </StackPanel>

                <StackPanel Grid.Column="4" VerticalAlignment="Center" Margin="10">
                    <Button Content="DQ" Focusable="False" Command="{Binding RedFighter.WinByDqCmd}" Background="#DA1E28" Foreground="White" FontWeight="Bold" Height="45"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ================= СИНИЙ БОЕЦ ================= -->
        <Border Grid.Row="1" Background="{Binding BlueFighter.RowBackground}" CornerRadius="4" Margin="0,5,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="160" />
                </Grid.ColumnDefinitions>

                <!-- Баллы -->
                <Grid Background="#0F62FE">
                    <TextBlock Text="{Binding BlueFighter.Points}" Foreground="White" FontSize="110" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,35"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,5">
                        <Button Content="+1" Focusable="False" Command="{Binding BlueFighter.ModifyPointsCmd}" CommandParameter="1" Width="45" Height="30" Margin="2" FontWeight="Bold"/>
                        <Button Content="+2" Focusable="False" Command="{Binding BlueFighter.ModifyPointsCmd}" CommandParameter="2" Width="45" Height="30" Margin="2" FontWeight="Bold"/>
                        <Button Content="-1" Focusable="False" Command="{Binding BlueFighter.ModifyPointsCmd}" CommandParameter="-1" Width="45" Height="30" Margin="2" FontWeight="Bold" Background="#002D9C" Foreground="White"/>
                    </StackPanel>
                </Grid>

                <!-- Автоматический блок Full Ippon -->
                <Grid Grid.Column="1" Background="#2A3B50" Margin="5,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="FULL IPPON (ИНДИКАТОРЫ)" Foreground="#8A99AD" FontSize="12" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,4,0,0"/>

                    <!-- Сетка с одинаковыми строками и колонками для идеального выравнивания -->
                    <Grid Grid.Row="1" Margin="5,3,5,5">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <ToggleButton Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" Content="P1 (+2)" IsChecked="{Binding BlueFighter.Part1Ippon}" Style="{StaticResource IpponToggle}"/>
                        <ToggleButton Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Content="P2 (+2)" IsChecked="{Binding BlueFighter.Part2Ippon}" Style="{StaticResource IpponToggle}"/>

                        <ToggleButton Grid.Row="0" Grid.Column="2" Content="P3 (+2)" IsChecked="{Binding BlueFighter.Part3Ippon2Points}" Style="{StaticResource IpponToggle}"/>
                        <ToggleButton Grid.Row="1" Grid.Column="2" Content="P3 (+3)" IsChecked="{Binding BlueFighter.Part3Ippon3Points}" Style="{StaticResource IpponToggle}"/>
                    </Grid>
                </Grid>

                <!-- Наказания -->
                <Grid Grid.Column="2" Background="#FFCC00" Margin="0,0,5,0">
                    <TextBlock Text="{Binding BlueFighter.Penalties}" Foreground="Black" FontSize="65" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,30"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,5">
                        <Button Content="+" Focusable="False" Command="{Binding BlueFighter.ModifyPenaltiesCmd}" CommandParameter="1" Width="40" Height="30" Margin="2" FontWeight="Bold"/>
                        <Button Content="-" Focusable="False" Command="{Binding BlueFighter.ModifyPenaltiesCmd}" CommandParameter="-1" Width="40" Height="30" Margin="2" FontWeight="Bold"/>
                    </StackPanel>
                    <TextBlock Text="Наказания" Foreground="#222" FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,38"/>
                </Grid>

                <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBox Text="{Binding BlueFighter.Name, UpdateSourceTrigger=PropertyChanged}" Tag="MainWin"
                             Foreground="White" FontSize="48" FontWeight="Bold" Background="Transparent" BorderThickness="0" CaretBrush="White"/>
                    <ComboBox ItemsSource="{Binding Teams}" SelectedItem="{Binding BlueFighter.Team}" Focusable="False"
                              Width="300" FontSize="24" HorizontalAlignment="Left" Margin="0,5,0,0" Background="#172637" Foreground="Black"/>
                </StackPanel>

                <StackPanel Grid.Column="4" VerticalAlignment="Center" Margin="10">
                    <Button Content="DQ" Focusable="False" Command="{Binding BlueFighter.WinByDqCmd}" Background="#DA1E28" Foreground="White" FontWeight="Bold" Height="45"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>